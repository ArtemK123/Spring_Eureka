spring:
  application:
    name: EurekaClient
     
 
---
eureka:
  instance:
    appname: EurekaClient
  client:   
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8000  